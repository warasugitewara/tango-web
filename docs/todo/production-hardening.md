# 本番化の計画（2026-09-14 時点）

プレリリースは単一 LXC 上の Docker Compose で動いている。人に使ってもらう前に、
**壊れたときに戻せること**を先に成立させる。順序はこの文書の並びどおり。

現状の構成と手順は README の「プレリリース配置」にある。ここでは足りないものだけを書く。

## 1. 復元が成立していること（2026-09-19 に達成）

2026-09-19 に実機で通した。dump 取得 2.1 秒、別データベースへの復元 3.4 秒、
復元後の行数は本番と完全一致（cards 327 / decks 10 / review_events 38 / principals 6）。
**バックアップから戻せることを実証済み。** 検証用DBは破棄し、本番の `tango` は無傷。

次は四半期ごとに繰り返す。前提が変わる（DB を分離する、PITR を入れる）たびにも通す。

- `infra/pre-release/restore.sh` は既定で `tango_restore_<日付>` という別データベースへ
  復元するため、本番に触れずに試せる。
- 2026-09-19 に実機を確認した時点では、**バックアップは1件も存在しなかった**。
  `/var/backups/tango` 自体が無く、root の cron も systemd timer も未設定で、
  README の手順3が一度も実行されていなかった。稼働開始から約1か月、
  実データ（デッキ・カード・学習履歴）を抱えたまま無保護だった。
- 同日、初回の dump を取得した。以後はこれが最後の復旧点になる。
- 手順は README の「4. 復元を試す」。次に通したときも所要時間をここへ追記する。

判定: 直近の dump から復元でき、テーブルが揃い、アプリを向けて起動できること。

## 2. 取得の自動化と置き場所（実機作業）

定期実行は 2026-09-19 に設定した。`tango-backup.timer`（systemd）が毎日 02:00 JST に
`backup.sh` を起動する。`Persistent=true` なので、停止していた間の分も起動後に1回走る。

残る穴は**置き場所**。同じ LXC の `/var/backups/tango` にしかない。LXC ごと失うと dump も消える。

- 別障害ドメインへ複製する。Proxmox Backup Server か NAS。
- 保持は仕様どおり daily 7 / weekly 4 / monthly 6。
- 複製の失敗を検知できるようにする（4 の監視と合わせる）。

## 3. App / DB の LXC 分離

仕様の目標構成は App LXC と DB LXC の2台で、PostgreSQL は内部インターフェイスのみ、
Proxmox Firewall で App LXC の固定アドレスからの 5432 だけを通す。TLS は
`sslmode=verify-full`、認証は SCRAM-SHA-256。

移行は「新しい DB LXC を作る → dump を復元 → App の `database_url` を差し替える →
旧 DB を一定期間残す」の順。切り替え前に 1 の復元手順で検証した dump を使う。

作業前に必要なもの:
- DB LXC の固定アドレス
- サーバ証明書（内部 CA でよい）と、App 側へ置くルート証明書
- 切り戻し手順（`database_url` を旧 DB へ戻すだけで済む状態を保つ）

## 4. pgBackRest と PITR

dump だけでは復旧点が最大24時間前になる。仕様の目標は RPO 15分 / RTO 2時間。

- WAL を別障害ドメインへ継続アーカイブし、週次フル＋日次差分。
- リポジトリは DB LXC 自身や同じローカルストレージに置かない。
- 四半期ごとに隔離インスタンスへ復元して、所要時間と結果を記録する。

これは 3 の分離後に入れる。単一 LXC のまま入れても、置き場所の要件を満たせない。

## 5. 監視と通知

- Uptime Kuma: Tunnel 越しに `/health/live` `/health/ready` と OAuth 開始パス。
  `/health/ready` は 2026-09-20 に実装した（`d5f83bb`）。DB へ届くことと、スキーマ定義が
  必要とするテーブルが揃っていることを見て、真偽だけを返す。不可は 503、理由は返さない。
  2026-09-20 に本番へ反映済みで、Tunnel 越しにも 200 を返す。あとは Uptime Kuma の
  監視先へ `/health/live` と `/health/ready` を登録する（未着手）。
- Zabbix: LXC 資源、コンテナ、PostgreSQL の接続数・長時間クエリ・容量、WAL 遅延、
  バックアップの鮮度、cloudflared、メンテナンスジョブの結果。
- Discord へ即時通知するのは、停止・DB 未 ready・バックアップ遅延。容量と証明書は warning。
  復旧通知も送る。

「バックアップの鮮度」を監視対象に入れるのが要点。1〜2 が動いていても、
止まったことに気付けなければ同じこと。

## 6. その他、本番前に片付けるもの

- OAuth シークレットのローテーション完了（`pre-release-deferred.md` の順序を守る）
- イメージの digest ピン留め（現在は tag 指定）
- デプロイ前バックアップの手順化（更新のたびに 1 を実行してから入れ替える）

切り戻し用の更新前イメージ: 2026-09-19 は `sha256:c7eb532e33fe`、2026-09-20 は
`sha256:e3d0bd5bfbba` から始まる ID。

## 記録

| 日付 | 何をしたか | 結果 |
| --- | --- | --- |
| 2026-09-14 | `restore.sh` を追加 | 未検証 |
| 2026-09-19 | 実機の状態を確認 | バックアップ0件、cron/timer 未設定と判明 |
| 2026-09-19 | 初回 `backup.sh` 実行 | 成功。63KB / 2.1 秒 |
| 2026-09-19 | dump の可読性を確認 | custom 形式、TOC 102 エントリ、TABLE DATA 16。中身の欠落は無し |
| 2026-09-19 | 復元リハーサル | 成功。3.4 秒。行数は本番と完全一致 |
| 2026-09-19 | 検証用DBを破棄 | 本番 `tango` は無傷を確認 |
| 2026-09-19 | 毎日 02:00 JST の timer を設定 | `tango-backup.timer` 有効。初回は 09-20 02:00 |
| 2026-09-19 | 本番を `2ca6640` へ更新 | 再ビルドして healthy。マイグレーションは 11 件のまま増えず、データ不変 |
| 2026-09-19 | `/health/ready` が 404 と判明 | 未実装。監視の前提が欠けている |
| 2026-09-20 | `/health/ready` を実装 | `d5f83bb` |
| 2026-09-20 | デプロイ前バックアップ | `tango-2026-09-20.dump` |
| 2026-09-20 | 本番を `7a47f54` へ更新 | healthy。ready は 200、データ不変（migrations 11 のまま） |
| 2026-09-21 | 自動バックアップの初回 | 02:00:11 に timer が起動し成功。3世代目 |
